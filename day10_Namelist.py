# names_with_length.py

# Store 5 names in a list
names = ["Marees", "Tamil", "Pranav", "Abi", "Mani"]

# Print each name and its length
print("Names and their lengths:\n"),
for name in names:
    print(f"{name} - {len(name)} characters")
